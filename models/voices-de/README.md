# German Kokoro voice embeddings for KokoroSharp (`.npy` `[N,1,256]`).

Do **not** copy these into `lib\voices`. Load them with **Voices Path** together with `models\kokoro-de.onnx`.

```powershell
scripts\download_german_pack.ps1
```
